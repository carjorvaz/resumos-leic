# Netlify PR-preview deployment (production is Cloudflare Pages).
# Repo config takes precedence over dashboard build settings.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
